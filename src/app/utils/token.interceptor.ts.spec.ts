import { TokenInterceptorTs } from './token.interceptor.ts';

describe('TokenInterceptorTs', () => {
  it('should create an instance', () => {
    expect(new TokenInterceptorTs()).toBeTruthy();
  });
});
